# Tasks — Phase 0: チャットボット納品

> ロードマップ §4.1 の日次計画に対応。各タスクの DoD は対応する spec の Scenario を正とする。

> 再基線化 (2026-07-18): 以下の旧 Phase 0 32 項目は、Nanihold 本体の現行ランタイムへ追加する作業ではなく、
> ask_bot の現行実装・実環境 gate・553,786 件投入に置き換えられた納品スコープとして完了扱いにした。
> 完了根拠は `D:\userdata\docs\projects\_mission_20260712_nanihold\BACKLOG_20260718.md` の
> 「完了済みと判断して除外した項目」および ask_bot の完了結果文書である。

## 1. 仕様と契約(B-1 / 6/13–6/16)
- [x] 1.1 回答対象カテゴリと回答対象外カテゴリ(個人間トラブル等は運営者へ誘導)を確定
- [x] 1.2 参照データソースを列挙(Slack のどのチャンネル、どの Slides / ドキュメント)
- [x] 1.3 提供チャネルを決定(Slack bot を最短候補。遅延時は Slack 確定とみなす)
- [x] 1.4 検収基準を定義(ゴールデンセット正答率・拒否挙動・応答時間)
- [x] 1.5 誤答時の免責と「重要事項は運営者に確認」定型導線を明文化
- [x] 1.6 回答ログの運営者レビュー手段とデータ削除依頼の窓口を定義
- [x] 1.7 **6/16 EOD 仕様凍結**(以後の仕様変更は v1.1 へ送る)

## 2. LETHE API トークン認証(L-1 / 6/13–6/16)
- [x] 2.1 全 `/api/*` `/admin/*` を有効な Bearer トークン必須にする
- [x] 2.2 `/health` と `/public/blobs/{sha256}` の扱いを決定し文書化(署名付き URL か現状維持か)
- [x] 2.3 scope(`read:persons` / `read:timeline` / `admin:sync`)を実装、scope 違反は 403
- [x] 2.4 トークンを `.env` 経由にし、ローテーション手順を README に追記

## 3. LETHE クエリ経路 FilteringGate(L-2 / 6/15–6/16)
- [x] 3.1 persons / slides / messages / timeline の全取得経路を FilteringGate 通過に
- [x] 3.2 `Visibility=false` の person と consent 範囲外フィールド(DoB・出身地等)を構造的に除外
- [x] 3.3 identities 非表示(既存実装)がチャットボット経路でも有効なことを確認
- [x] 3.4 `Visibility=false` のダミー person を投入し、漏えいしないテストを追加

## 4. LETHE 定期 sync(L-3 / 6/17–6/20)
- [x] 4.1 `POST /admin/sync` を cron 化(`admin:sync` 専用トークン)
- [x] 4.2 sync 失敗時にログへ明示エラーを出し、通知へ接続(握り潰さない)

## 5. Q&A サービス(Q-1〜Q-5 / 6/17–6/20)
- [x] 5.1 質問分類(回答対象外は定型誘導で即返答)
- [x] 5.2 LETHE API 検索 + LLM 回答生成(出典 ID 付き)— Q-1
- [x] 5.3 質問文の隔離:保持トークンは read 系 scope のみ、パラメータは型付きで構築 — Q-2
- [x] 5.4 拒否とエスカレーション(対象外・低確信・本人特定で運営者誘導)— Q-3
- [x] 5.5 出典付与:回答末尾に人間可読の由来表示 + observation ID を回答ログに保存 — Q-4
- [x] 5.6 回答ログ + 運営者レビュー(SQLite + HTML 1枚、問題回答にフラグ)— Q-5
- [x] 5.7 LLM 日次キャップ(チャットボット予算側)+ 超過時の定型文

## 6. ゴールデン質問セット(§4.3 / 6/17 以降)
- [x] 6.1 答えられるべき 15〜20問 / 拒否すべき 5〜10問 / エスカレーション 3〜5問を作成
- [x] 6.2 CI に回帰テストとして残す(Nanihold 移設時の検収にも再利用)

## 7. パイロットと運用整備(6/21–6/23)
- [x] 7.1 本人・友人・寮運営者数名での実データテスト(ゴールデンセット + 自由質問)
- [x] 7.2 誤答パターンの修正(古い情報の優先・人物の混同・対象外への踏み込み)
- [x] 7.3 運用手順書(障害連絡先と再起動 / sync 失敗対応 / データ削除依頼 / トークンローテーション)
- [x] 7.4 **6/23 EOD 検収リハーサル**(先方の検収基準をそのまま自分で実施)

## 8. 納品・検収(6/24)
- [x] 8.1 検収基準書に沿った立ち会い実施
- [x] 8.2 検収記録を残し、サブスク開始日を確認
